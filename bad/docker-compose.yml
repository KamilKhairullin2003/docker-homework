version: "3.9"

services:
  app:
    build: .
    container_name: my_app
    ports:
      - "8000:8000"
    environment:
      - DB_PASSWORD=123456
    depends_on:
      - db

  db:
    image: postgres:latest
    ports:
      - "5432:5432"
